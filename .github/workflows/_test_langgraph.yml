name: test

on:
  workflow_call:

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-2025]
        python-version:
          - "3.9"
          - "3.10"
          - "3.11"
          - "3.12"
          - "3.13"

    defaults:
      run:
        working-directory: libs/langgraph
    name: "test ${{ matrix.os }} #${{ matrix.python-version }}"
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python ${{ matrix.python-version }}
        uses: astral-sh/setup-uv@v6
        with:
          python-version: ${{ matrix.python-version }}
          enable-cache: true
          cache-suffix: "test-langgraph"

      # Windows runners have make pre-installed
      # No MSYS2 setup needed

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        if: ${{ !github.event.pull_request.head.repo.fork }}
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_RO_TOKEN }}

      - name: Install dependencies
        shell: bash
        run: uv sync --frozen --group dev

      - name: Set up PostgreSQL (Ubuntu only)
        if: matrix.os == 'ubuntu-latest'
        run: |
          sudo systemctl start postgresql.service
          sudo -u postgres createuser --createdb --superuser runner
          sudo -u postgres psql -c "ALTER USER runner PASSWORD 'password';"
          createdb test -h localhost -U runner

      - name: Run tests (Ubuntu)
        if: matrix.os == 'ubuntu-latest'
        shell: bash
        run: make test_parallel
        env:
          POSTGRES_HOST: localhost
          POSTGRES_USER: runner
          POSTGRES_PASSWORD: password
          POSTGRES_DB: test

      - name: Run tests (Windows)
        if: matrix.os == 'windows-2025'
        shell: bash
        run: NO_DOCKER=true make test_parallel

      - name: Ensure the tests did not create any additional files
        shell: bash
        run: |
          set -eu

          STATUS="$(git status)"
          echo "$STATUS"

          # grep will exit non-zero if the target message isn't found,
          # and `set -e` above will cause the step to fail.
          echo "$STATUS" | grep 'nothing to commit, working tree clean'
